# class Reminder:
#     def __init__(self, id, appointment_id, reminder_date, reminder_message):
#         self.id = id
#         self.appointment_id = appointment_id
#         self.reminder_date = reminder_date
#         self.reminder_message = reminder_message

#     def __repr__(self):
#         return f"Reminder(id={self.id}, appointment_id={self.appointment_id}, reminder_date='{self.reminder_date}', reminder_message='{self.reminder_message}')"
